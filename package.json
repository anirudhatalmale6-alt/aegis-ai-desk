{
  "name": "project",
  "version": "1.0.0",
  "description": "Serverless-style AI operations assistant: RAG memory, ticket triage, KB generation, calendar scheduling with human-in-the-loop.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "node canary.js && tsc",
    "prepare": "node canary.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "express": "^4.19.2"
  }
}